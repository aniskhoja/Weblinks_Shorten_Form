<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>
    <div class="container">
    <header style="margin: auto; text-align: center;">
        <h1>Weblinks</h1>
        <h5>We will shorten you weblink</h5>
    </header>
    <br/>
        <form action="/url" method="POST">
            <div style="width: 50%; margin: auto;" class="input-group">
                <input style="display: inline;" class="form-control form-control-lg" style=" width:50%" type="text" name='url' required placeholder="Enter your web URL">
                <button type="submit" class="btn btn-primary m2-3">submit</button>
            </div>
            
            <table class="table">
                <thead>
                    <th>Full Url</th>
                    <th>Short Url</th>
                    <th>click</th>
                </thead>
                <tbody>
                    <% url.forEach(url => { %>
                        <tr>
                        <td><a href="<%= url.shortUrl %>">
                        <%= url.url %> </a> </td>
                        <td><a href="<%= url.shortUrl %>"><%= url.shortUrl %></a> </td>
                        <td><%= url.click %></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
            <% if(data.previous !== undefined) {%>
                <a href="/?page=<%= data.previous.page %>&limit=4">
                <button type="button">
                        <%= data.previous.page %></button>
                    </a>
            <% } %>
            <% if(data.next !== undefined) { %>
                <a href="/?page=<%= data.next.page %>&limit=4">
                <button type="button">
                        <%= data.next.page %>
                    </button>
                </a>
           <% } %>
        </form>
    </div>
</body>
</html>